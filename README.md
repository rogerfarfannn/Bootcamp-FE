for this challenge I had to use AI multiple times.

First I didn't know how to make the layout change from the signal when it was clicked, so the ai implemented this:

<div class="layout" [class.sidebar-collapsed]="!isOpen()">

which I understood and know I know.

Then for the responsive mobile view, I knew it was using @media on the css, I just didn't know the dimension for mobile so I asked that
