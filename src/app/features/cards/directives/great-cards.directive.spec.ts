import { GreatCardsDirective } from './great-cards.directive';

describe('GreatCardsDirective', () => {
  it('should create an instance', () => {
    const directive = new GreatCardsDirective();
    expect(directive).toBeTruthy();
  });
});
